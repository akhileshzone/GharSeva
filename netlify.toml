# Netlify root config (repo root publish = frontend)
[build]
  base = "frontend"
  publish = "."
  command = "node inject-config.js"

[build.environment]
  NODE_VERSION = "20"

# Set in Netlify UI:
#   API_BASE_URL = https://YOUR-RENDER-API.onrender.com
