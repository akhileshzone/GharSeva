# Render Blueprint (repo root)
# Dashboard → New → Blueprint → connect this GitHub repo

services:
  - type: web
    name: ghar-seva-api
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install && npx prisma generate && npx prisma db push
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: ghar-seva-db
          property: connectionString
      - key: CORS_ORIGINS
        value: https://charming-horse-0b7ee8.netlify.app

databases:
  - name: ghar-seva-db
    plan: free
    databaseName: gharseva
    user: gharseva
